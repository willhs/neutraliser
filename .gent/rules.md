# Agent Guardrails

- Respect human-owned files flagged in `docs/philosophy/`.
- Log every non-trivial change in the Change Log of the touched doc.
- Keep generated content ASCII unless the project explicitly opts in.
