# Agent Guidelines

- Prefer worktrees or task-specific branches for multi-file edits.
- Run `bundle exec rspec` and `bundle exec rubocop` before handing work back.
- Preserve doc front matter (`type`, `status`, `updated`, etc.) and append Change Log entries on every edit.
