# Form
A CLI, written in Ruby. 

Pipeline:
- adjusts the volume of selected media to a reasonable and consistent (to your library) level
- creates a copy, or if a flag is used replaces the original media file

